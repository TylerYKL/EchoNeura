# Where the Next dev server proxies /api/* (server-side only; never shipped to the browser).
BACKEND_URL=http://127.0.0.1:8000

# Extra dev origins allowed to load this app (comma-separated).
# *.e2b.app is already allowed by default for the Arena live preview.
ALLOWED_DEV_ORIGINS=
